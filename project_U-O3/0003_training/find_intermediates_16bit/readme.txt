Target: to calculate the 16-bit intermediate values we need in each round of initialization and finalization for template profiling.

a) We should finish the 8-bit version (find_intermediates/) first.

b) all-in-one script:
	./readme.sh


